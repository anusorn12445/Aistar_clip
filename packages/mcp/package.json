{
  "name": "@aistar/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "AISTAR Talent OS MCP server — read + draft_write scope over the REST API",
  "bin": {
    "aistar-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
